# Макрос для удобного оперделения функций
# Определяя подпрограммы таким образом тело функции оборачивается в макрос, что делает метки локальными.
# Также это позволяет не писать пролог и эпилог в каждой функции
.macro func (%name, %body)
.text
	%name:
	# Пролог
	sw ra (sp)
	addi sp sp -4
	
	# Также можно сохранять s-регистры (тут сохраняется только s0, просто для примера, но можно было бы сохранять и все)
	# Закомментировано, так как ни в каких подпрограммах конкретно в данном домашнем задании s-регистры не используются
	# sw s0 (sp)
	# addi sp sp -4
	
	%body ()
	# Восстанавливаем s0
	# lw s0 4(sp)
	# addi sp sp 4
	
	# Эпилог 
	lw ra 4(sp)
	addi sp sp 4
	ret
.end_macro
